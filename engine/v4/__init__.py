"""engine/v4 — AI-directed cinematography upgrade (directive 2026-08-30).

Modules:
    planner      — two-stage §5 planning hierarchy, shot classes (§12),
                   show-don't-label (§13/§24), pattern interrupts (§14).
    microevents  — Shot → MicroEvents vocabulary + per-renderer mapping.
    gates        — artifact-measuring publish gates (§19).
    critic       — human-like creative critic (§20).
    reedit       — re-edit operations, trim-first order (§21).
"""
